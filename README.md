# Image-Rename-and-EXIF-Exporter
this program renames an image to include its original date taken as well as generates a docx file containing the image and its exif data.

uses python-docx, PILLOW, tkinter
